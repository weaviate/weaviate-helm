{{ if .Values.mcpService.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mcpService.name }}
  labels:
    app.kubernetes.io/name: weaviate
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  {{- with .Values.mcpService.annotations }}
  annotations: {{ toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.mcpService.type }}
  selector:
    app: weaviate
  ports:
    {{- range .Values.mcpService.ports }}
    - {{ toYaml . | indent 6 | trim }}
      targetPort: {{ .port }}
    {{- end }}
{{ if eq .Values.mcpService.type "ClusterIP" -}}
  {{ if .Values.mcpService.clusterIP }}
  clusterIP: {{ .Values.mcpService.clusterIP }}
  {{ end }}
{{ end }}
{{ if eq .Values.mcpService.type "LoadBalancer" -}}
  {{- if gt (len .Values.mcpService.loadBalancerSourceRanges) 0 }}
  loadBalancerSourceRanges:
    {{- range $_, $sourceRange := .Values.mcpService.loadBalancerSourceRanges }}
    - {{ $sourceRange }}
    {{ end -}}
  {{- end -}}
{{- end }}
{{ end }}
